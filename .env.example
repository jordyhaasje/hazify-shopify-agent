# Copy to .env only if a local tool explicitly requires it.
# Do not commit .env or paste these values into chat.

SHOPIFY_STORE=example.myshopify.com
SHOPIFY_ADMIN_API_TOKEN=
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
HAZIFY_CREDENTIAL_PASSPHRASE=
